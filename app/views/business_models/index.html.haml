%h1 Listing business_models

%table
  %thead
    %tr
      %th Name
      %th Description
      %th Slug
      %th
      %th
      %th

  %tbody
    - @business_models.each do |business_model|
      %tr
        %td= business_model.name
        %td= business_model.description
        %td= business_model.slug
        %td= link_to 'Show', business_model
        %td= link_to 'Edit', edit_business_model_path(business_model)
        %td= link_to 'Destroy', business_model, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Business model', new_business_model_path
