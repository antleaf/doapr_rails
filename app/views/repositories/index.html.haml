%h4.page-title All Repositories

%div.row
  %div.col-lg-6
    - if policy(Repository).create?
      = link_to 'New Repository', new_repository_path, :class => "btn btn-info btn-sm"
  %div.col-lg-6.right-aligned
    -# = render "_shared/download_machine_readable_data_buttons"

= render(:partial => 'table',:locals => {:repository_list => @repositories})

%div.row
  %div.col-lg-6
    - if policy(Repository).create?
      = link_to 'New Repository', new_repository_path, :class => "btn btn-info btn-sm"
  %div.col-lg-6.right-aligned
    -# = render "_shared/download_machine_readable_data_buttons"