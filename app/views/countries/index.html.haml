%h4.page-title All Countries

%div.row
  %div.col-lg-6
    - if policy(Country).create?
      = link_to 'New Country', new_country_path, :class => "btn btn-secondary btn-sm"
  %div.col-lg-6.right-aligned
    -# = render "_shared/download_machine_readable_data_buttons"

= render(:partial => 'table',:locals => {:country_list => @countries})

%div.row
  %div.col-lg-6
    - if policy(Country).create?
      = link_to 'New Country', new_country_path, :class => "btn btn-info btn-sm"
  %div.col-lg-6.right-aligned
    -# = render "_shared/download_machine_readable_data_buttons"


